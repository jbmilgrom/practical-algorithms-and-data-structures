from graphs.generate_parenthesis.method import generate_parenthesis
# from graphs.anagram.test import factorial

# def count_valid_expressions(n):
#     duplicate_open_permutations = factorial(n)
#     duplicaate_close_permutations = duplicate_open_permutations

#     return factorial(n * 2) / (duplicaate_close_permutations + duplicate_open_permutations)


print(generate_parenthesis(3))

# assert expected == len(received), "Expected {} exprs, but received {}".format(expected, received)